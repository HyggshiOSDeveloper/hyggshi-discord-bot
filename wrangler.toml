name = "hyggshi-discord-bot"
main = "src/index.js"
compatibility_date = "2024-01-01"

[vars]
# Các biến môi trường public (không phải secret)

# Nếu cần secrets (Discord token, etc.)
# Dùng: wrangler secret put DISCORD_TOKEN
